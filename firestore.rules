rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /bookings/{bookingId} {
      // TESTING ONLY: this lets the two static GitHub Pages sites
      // read/write bookings. For production, add Firebase Authentication
      // and restrict writes to an authenticated admin.
      allow read, write: if true;
    }
  }
}
